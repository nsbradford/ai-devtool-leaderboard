'use client';

import LeaderboardChart from '@/components/LeaderboardChart';

/**
 * Home page component that renders the AI Code Review Adoption Tracker.
 * Displays the main leaderboard chart showing tool adoption trends.
 * @returns JSX element containing the home page
 */
export default function Home() {
  return <LeaderboardChart />;
}
